apiVersion: v1
kind: Service
metadata:
  name: svc-{{WORKSPACE_ID}}
  namespace: cloud-ide
  labels:
    app: svc-runner
    ws: {{WORKSPACE_ID}}
spec:
  selector:
    app: svc-runner
    ws: {{WORKSPACE_ID}}
  ports:
  - name: http
    port: 80
    targetPort: 5173
  type: ClusterIP
    